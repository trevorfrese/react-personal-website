import React from 'react';

import classes from './RoleModels.css';

const roleModels = (props) => (
  <div className={classes.RoleModels}>
    Insert role models here
  </div>
);

export default roleModels;
